{
  "name": "schoollaider",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev": "pnpm run dev:backend & pnpm run dev:frontend",
    "build": "pnpm --filter shared build && pnpm --filter backend build && pnpm --filter frontend build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["msgpackr-extract", "@prisma/client", "@prisma/engines", "esbuild", "prisma"]
  }
}
