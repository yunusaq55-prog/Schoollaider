{
  "framework": "vite",
  "installCommand": "cd .. && pnpm install",
  "buildCommand": "cd .. && pnpm --filter shared build && pnpm --filter frontend build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
